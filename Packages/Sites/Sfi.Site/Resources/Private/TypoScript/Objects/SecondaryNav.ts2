prototype(Sfi.Site:SecondaryNav) < prototype(T:Template) {
	templatePath = 'resource://Sfi.Site/Private/TypoScript/Objects/SecondaryNav.html'
	# TODO: add search
	@if.isRus = ${lang == 'ru' ? true : false}
}
